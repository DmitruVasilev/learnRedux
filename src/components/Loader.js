import React from 'react'
import PropTypes from 'prop-types'

const Loader = ({props}) => {
  return (
    <div>
      <h2>Loading...</h2>
    </div>
  )
}

Loader.propTypes = {
  // test: PropTypes.string
}

export default Loader